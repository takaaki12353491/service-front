import React, { useState } from 'react'
import Header from '../organisms/Header'

interface Props {
}

const Home: React.FC<Props> = (props) => {
  return(
    <Header></Header>
  )
}
export default Home